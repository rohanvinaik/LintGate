=== ISSUE 157 ===
title:	P0: Define cross-architecture behavioral delivery protocol and host routing metadata
state:	OPEN
author:	rohanvinaik
labels:	area:agent-integration, area:quality, controlplane, P0, type:enhancement
comments:	0
assignees:	
projects:	
milestone:	
number:	157
--
## Parent
Part of #156 (Phase 3: unified delivery abstraction).

## Category
A (execution-ready) — protocol and capability contract.

## One Deliverable
Define a unified behavioral delivery abstraction (`DeliveryChannel`) and extend host capability metadata for delivery routing.

## Scope (files likely touched)
- `lintgate/orchestration/delivery.py` (new)
- `lintgate/renderers/host_adapter.py`
- `lintgate/renderers/__init__.py`
- `tests/test_delivery_channels.py` (new)
- `tests/test_host_adapters.py`

## Implementation contract
1. Add `DeliveryChannel` protocol with required fields/methods:
   - `channel_type`
   - `reliability`
   - `latency`
   - `format_finding(...)`
   - `format_repertoire_hint(...)`
   - `supports_proactive_push()`
2. Extend `HostCapabilities` with delivery-routing metadata (additive fields only), including preferred delivery channels and intervention support.
3. Add deterministic host->channel resolution helper (ordered by reliability).
4. Keep existing renderer contracts backward-compatible.

## Adversarial requirements
- Unknown host must resolve to deterministic fallback channel set (no exception).
- Missing delivery metadata on legacy capability presets must default safely.

## Acceptance commands
```bash
pytest tests/test_host_adapters.py tests/test_delivery_channels.py -q
python - <<'PY'
from lintgate.orchestration.delivery import DeliveryChannel
print(hasattr(DeliveryChannel, '__dict__'))
PY
```
Expected:
- Tests pass.
- Host capability presets remain import-compatible.

## Dependency metadata
- `depends_on`: none
- `parallel_safe_with`: #160, #162, #166, #168
- `conflicts_with`: concurrent edits to `lintgate/renderers/host_adapter.py`

## Out of scope
- Hook/MCP integration behavior.

=== ISSUE 158 ===
title:	P0: Implement delivery channel adapters and deterministic fallback routing
state:	OPEN
author:	rohanvinaik
labels:	area:agent-integration, area:quality, controlplane, P0, type:enhancement
comments:	0
assignees:	
projects:	
milestone:	
number:	158
--
## Parent
Part of #156 (Phase 3: unified delivery abstraction).

## Category
A (execution-ready) — concrete adapter formatters.

## One Deliverable
Implement delivery adapters for Claude Code, Gemini CLI, Cline, Cursor, MCP-only, and open-weight conversation channels with deterministic fallback routing.

## Scope (files likely touched)
- `lintgate/orchestration/delivery.py`
- `tests/test_delivery_channels.py`

## Implementation contract
1. Implement formatter adapters with channel-specific framing:
   - `ClaudeCodeChannel`
   - `GeminiCliChannel`
   - `ClineChannel`
   - `CursorChannel`
   - `McpOnlyChannel`
   - `OpenWeightChannel`
2. Add `deliver_finding(...)` fallback chain:
   - resolve channels by host capability
   - choose first channel compatible with authority level
   - deterministic fallback to lowest-reliability nudge channel
3. Ensure formatted payload includes machine-readable metadata:
   - `authority_level`
   - `delivery_channel`
   - `reliability`

## Adversarial requirements
- Unsupported authority/channel combinations must gracefully downgrade (not fail).
- Formatter output must be deterministic byte-for-byte for same inputs.

## Acceptance commands
```bash
pytest tests/test_delivery_channels.py -q
python - <<'PY'
from lintgate.orchestration.delivery import deliver_finding
print(callable(deliver_finding))
PY
```
Expected:
- Tests pass.
- Fallback routing produces deterministic channel selection.

## Dependency metadata
- `depends_on`: #157
- `parallel_safe_with`: #160, #162, #166, #168
- `conflicts_with`: concurrent edits to `lintgate/orchestration/delivery.py`

## Out of scope
- Proactive MCP response enrichment and hook wiring.

=== ISSUE 159 ===
title:	P0: Wire unified delivery into hooks and proactive MCP behavioral status push
state:	OPEN
author:	rohanvinaik
labels:	area:agent-integration, controlplane, developer-experience, P0, type:enhancement
comments:	0
assignees:	
projects:	
milestone:	
number:	159
--
## Parent
Part of #156 (Phase 3: unified delivery abstraction + proactive push).

## Category
A (execution-ready) — MCP/hook integration.

## One Deliverable
Wire delivery abstraction into runtime paths and add proactive behavioral status push for MCP-only hosts.

## Scope (files likely touched)
- `lintgate/hook_posttooluse.py`
- `lintgate/hook_runtime_state.py`
- `mcp_tools/micro_refresh.py`
- `mcp_server.py`
- `tests/test_hook_posttooluse.py`
- `tests/test_hook_runtime_state_cov.py`
- `tests/test_micro_refresh.py`

## Implementation contract
1. Route hook behavioral messages through delivery abstraction instead of raw single-format system text.
2. Add MCP proactive status line attachment in tool responses when behavioral observations are pending:
   - additive field under `session_context` or sibling `behavior_status`
   - include pending count and follow-up tool hint
3. For rule-file-capable hosts, write compact behavioral delivery artifacts between completions via runtime dynamic file path.
4. Maintain backward compatibility for existing Claude PostToolUse schema.

## Adversarial requirements
- If delivery adapter fails, fallback to existing behavior (never drop response).
- Proactive status push must not appear when no behavioral observations are pending.

## Acceptance commands
```bash
pytest tests/test_hook_posttooluse.py tests/test_hook_runtime_state_cov.py tests/test_micro_refresh.py -q
python - <<'PY'
import mcp_server
print(callable(mcp_server._json_dumps))
PY
```
Expected:
- Tests pass.
- MCP responses include behavior status only when pending.

## Dependency metadata
- `depends_on`: #158
- `parallel_safe_with`: #160, #162, #166, #168
- `conflicts_with`: concurrent edits to `lintgate/hook_posttooluse.py` and `mcp_server.py`

## Out of scope
- Authority escalation policy logic.

=== ISSUE 160 ===
title:	P0: Add authority escalation engine with model-risk and significance modulation
state:	OPEN
author:	rohanvinaik
labels:	area:quality, controlplane, metrics, P0, type:enhancement
comments:	0
assignees:	
projects:	
milestone:	
number:	160
--
## Parent
Part of #156 (Phase 4: graduated authority model).

## Category
A (execution-ready) — authority core policy.

## One Deliverable
Implement authority levels and escalation engine with significance and model-risk modulation.

## Scope (files likely touched)
- `lintgate/orchestration/authority.py` (new)
- `lintgate/controlplane/model_profiles.py`
- `tests/test_authority_escalation.py` (new)
- `tests/test_model_profiles.py`

## Implementation contract
1. Add `AuthorityLevel` enum: `nudge`, `advisory`, `warning`, `intervention`.
2. Implement deterministic escalation function using:
   - persistence count
   - signal severity baseline
   - model risk multiplier
   - significance attribution cap (`model_tendency`/`codebase_tendency` cap at advisory)
3. Add stable metadata payload:
   - `authority_level`
   - `escalation_reason`
   - `escalation_count`
4. Keep computation pure (no IO/network).

## Adversarial requirements
- Unknown significance attribution must default to conservative escalation.
- Escalation must be monotonic within a session unless explicit reset trigger provided.

## Acceptance commands
```bash
pytest tests/test_authority_escalation.py tests/test_model_profiles.py -q
python - <<'PY'
from lintgate.orchestration.authority import AuthorityLevel
print(AuthorityLevel.NUDGE.value == 'nudge')
PY
```
Expected:
- Tests pass.
- Escalation behavior deterministic across repeated calls.

## Dependency metadata
- `depends_on`: none
- `parallel_safe_with`: #157, #162, #166, #168
- `conflicts_with`: concurrent edits to `lintgate/controlplane/model_profiles.py`

## Out of scope
- Delivery-channel formatting integration.

=== ISSUE 161 ===
title:	P1: Integrate authority levels into behavior findings, arbitration, and delivery formatting
state:	OPEN
author:	rohanvinaik
labels:	area:agent-integration, controlplane, developer-experience, P1, type:enhancement
comments:	0
assignees:	
projects:	
milestone:	
number:	161
--
## Parent
Part of #156 (Phase 4: graduated authority model).

## Category
A (execution-ready) — behavior channel + arbitration integration.

## One Deliverable
Integrate authority levels into behavior findings, arbitration force-emit rules, and delivery formatting paths.

## Scope (files likely touched)
- `lintgate/channels/behavior_channel.py`
- `lintgate/hook_arbitration.py`
- `lintgate/orchestration/delivery.py`
- `tests/test_behavior_channel.py`
- `tests/test_hook_arbitration_cov.py`
- `tests/test_delivery_channels.py`

## Implementation contract
1. Behavior findings carry `authority_level` metadata from escalation engine.
2. Arbitration force-emit checks incorporate authority (`warning`/`intervention` always emitted in pulse/silent).
3. Delivery formatter uses authority-specific framing contracts (nudge/advisory/warning/intervention).
4. Ensure authority metadata is preserved in metrics and serialized outputs.

## Adversarial requirements
- Informational low-significance signals must never be framed as intervention.
- Existing disposition injection behavior must remain compatible with authority metadata.

## Acceptance commands
```bash
pytest tests/test_behavior_channel.py tests/test_hook_arbitration_cov.py tests/test_delivery_channels.py -q
python - <<'PY'
from lintgate.hook_arbitration import arbitrate_output
print(callable(arbitrate_output))
PY
```
Expected:
- Tests pass.
- Intervention-level findings force emit under non-full verbosity.

## Dependency metadata
- `depends_on`: #160, #158
- `parallel_safe_with`: #162, #166, #168
- `conflicts_with`: concurrent edits to `lintgate/channels/behavior_channel.py`

## Out of scope
- Compliance-rate feedback adjustments.

=== ISSUE 162 ===
title:	P1: Add persistent resolution repertoire capture from session finding deltas
state:	OPEN
author:	rohanvinaik
labels:	area:quality, controlplane, metrics, P1, type:enhancement
comments:	0
assignees:	
projects:	
milestone:	
number:	162
--
## Parent
Part of #156 (Phase 1: successful pattern repertoire).

## Category
A (execution-ready) — resolution record capture and persistence.

## One Deliverable
Add `ResolutionRecord` capture in session memory and persistent project repertoire storage/query.

## Scope (files likely touched)
- `lintgate/controlplane/session_memory.py`
- `lintgate/orchestration/resolution_repertoire.py` (new)
- `tests/test_session_memory.py`
- `tests/test_resolution_repertoire.py` (new)

## Implementation contract
1. Add `ResolutionRecord` schema with stable serialized fields.
2. Detect candidate resolutions from finding-index deltas across snapshots:
   - finding persisted 3+ snapshots, then disappears
   - extract tool/action sequence from resolution window
3. Persist repertoire per project with bounded LRU window (max 50 records).
4. Provide deterministic `query_repertoire(trigger_signature)` API.

## Adversarial requirements
- Multi-finding disappearance events must still record low-confidence candidates (not dropped silently).
- Corrupted repertoire file must fail-open to empty repertoire.

## Acceptance commands
```bash
pytest tests/test_session_memory.py tests/test_resolution_repertoire.py -q
python - <<'PY'
from lintgate.orchestration.resolution_repertoire import query_repertoire
print(callable(query_repertoire))
PY
```
Expected:
- Tests pass.
- Resolution records persist across session expiry boundaries.

## Dependency metadata
- `depends_on`: none
- `parallel_safe_with`: #157, #160, #166, #168
- `conflicts_with`: concurrent edits to `lintgate/controlplane/session_memory.py`

## Out of scope
- Surfacing repertoire hints in behavior findings.

=== ISSUE 163 ===
title:	P1: Surface proven resolution hints in behavior findings and controlplane details
state:	OPEN
author:	rohanvinaik
labels:	area:agent-integration, area:quality, controlplane, P1, type:enhancement
comments:	0
assignees:	
projects:	
milestone:	
number:	163
--
## Parent
Part of #156 (Phase 1: successful pattern repertoire).

## Category
A (execution-ready) — finding/report integration.

## One Deliverable
Surface proven resolution hints from repertoire in behavior findings and ControlPlane detail drill-down.

## Scope (files likely touched)
- `lintgate/channels/behavior_channel.py`
- `lintgate/controlplane/reporter_compact.py`
- `mcp_tools/controlplane_tools.py`
- `tests/test_behavior_channel.py`
- `tests/test_reporter_compact.py`
- `tests/test_controlplane_tools.py`

## Implementation contract
1. On matching trigger signatures, attach `proven_resolution` block to behavior findings.
2. Include `proven_resolutions` section in `controlplane_get_details` output when available.
3. Keep integration additive and deterministic (no changes to existing required keys).
4. Include confidence field in surfaced repertoire hints.

## Adversarial requirements
- Missing repertoire match must not modify finding payload structure beyond optional field absence.
- Low-confidence resolution hints must be marked as low-confidence (never presented as authoritative).

## Acceptance commands
```bash
pytest tests/test_behavior_channel.py tests/test_reporter_compact.py tests/test_controlplane_tools.py -q
python - <<'PY'
print(True)
PY
```
Expected:
- Tests pass.
- Matching behavior findings include proven-resolution hints.

## Dependency metadata
- `depends_on`: #162
- `parallel_safe_with`: #166, #168
- `conflicts_with`: concurrent edits to `lintgate/channels/behavior_channel.py`

## Out of scope
- Authority escalation from compliance rates.

=== ISSUE 164 ===
title:	P1: Introduce structured nudge outcome events and deterministic compliance classification
state:	OPEN
author:	rohanvinaik
labels:	area:quality, controlplane, metrics, P1, type:enhancement
comments:	0
assignees:	
projects:	
milestone:	
number:	164
--
## Parent
Part of #156 (Phase 5: compliance feedback loop).

## Category
A (execution-ready) — outcome schema and detection windows.

## One Deliverable
Introduce structured `NudgeOutcome` events and deterministic followed/ignored/overridden detection logic.

## Scope (files likely touched)
- `lintgate/controlplane/global_behavior_profile.py`
- `lintgate/channels/behavior_channel.py`
- `lintgate/orchestration/compliance_feedback.py` (new)
- `tests/test_global_behavior_profile.py`
- `tests/test_compliance_feedback.py` (new)

## Implementation contract
1. Add structured outcome schema with fields:
   - `signal`, `authority_level`, `timestamp`, `outcome`, `latency_events`, `context`, `delivery_channel`
2. Implement window rules:
   - followed within 3 events
   - ignored within 5 events
   - overridden on explicit contradictory action
3. Persist aggregated counts and recent outcome samples per signal.
4. Keep existing `nudge_outcomes` compatibility by additive extension/migration logic.

## Adversarial requirements
- Ambiguous event windows must resolve to `ignored` deterministically at timeout.
- Missing event history must not crash; outcome detection returns empty result.

## Acceptance commands
```bash
pytest tests/test_global_behavior_profile.py tests/test_compliance_feedback.py -q
python - <<'PY'
from lintgate.orchestration.compliance_feedback import classify_nudge_outcome
print(callable(classify_nudge_outcome))
PY
```
Expected:
- Tests pass.
- Outcome classification stable across repeated replays.

## Dependency metadata
- `depends_on`: #161
- `parallel_safe_with`: #166, #168
- `conflicts_with`: concurrent edits to `lintgate/controlplane/global_behavior_profile.py`

## Out of scope
- Escalation policy changes based on compliance rates.

=== ISSUE 165 ===
title:	P1: Feed compliance rates back into escalation, cadence, and reinforcement behavior
state:	OPEN
author:	rohanvinaik
labels:	controlplane, developer-experience, metrics, P1, type:enhancement
comments:	0
assignees:	
projects:	
milestone:	
number:	165
--
## Parent
Part of #156 (Phase 5: compliance feedback loop).

## Category
A (execution-ready) — adaptive delivery behavior.

## One Deliverable
Feed compliance rates back into authority and delivery cadence, with bounded positive reinforcement and suppression on persistent ignore.

## Scope (files likely touched)
- `lintgate/orchestration/authority.py`
- `lintgate/orchestration/delivery.py`
- `lintgate/channels/behavior_channel.py`
- `tests/test_authority_escalation.py`
- `tests/test_compliance_feedback.py`
- `tests/test_delivery_channels.py`

## Implementation contract
1. Implement compliance-rate policy:
   - >70%: reduce nudge cadence
   - 30-70%: baseline cadence
   - <30% after 5+ nudges: +1 authority level
   - <10% after 10+ nudges: suppress nudges for that signal
2. Add bounded positive reinforcement on followed nudges (max 1 per 10 events).
3. Emit diagnostics metadata:
   - `compliance_rate`
   - `suppressed_nudge`
   - `reinforcement_emitted`
4. Keep advisory-only behavior; no hard blocking introduced.

## Adversarial requirements
- Suppression must be per-signal, not global.
- Reinforcement text must never repeat on consecutive events.

## Acceptance commands
```bash
pytest tests/test_authority_escalation.py tests/test_compliance_feedback.py tests/test_delivery_channels.py -q
python - <<'PY'
print(True)
PY
```
Expected:
- Tests pass.
- Low-compliance signals escalate; ultra-low-compliance signals suppress nudges.

## Dependency metadata
- `depends_on`: #164
- `parallel_safe_with`: #166, #168
- `conflicts_with`: concurrent edits to `lintgate/orchestration/authority.py`

## Out of scope
- Cross-session transfer packet generation.

=== ISSUE 166 ===
title:	P2: Implement signal-source decomposition (model/codebase/session) and behavioral baselines
state:	OPEN
author:	rohanvinaik
labels:	area:quality, controlplane, metrics, P2, type:enhancement
comments:	0
assignees:	
projects:	
milestone:	
number:	166
--
## Parent
Part of #156 (Phase 2: signal significance discrimination).

## Category
B (mixed theory/execution) — baseline decomposition model.

## One Deliverable
Implement signal-source decomposition (model baseline vs codebase baseline vs session deviation) and persist behavioral baseline stats.

## Scope (files likely touched)
- `lintgate/controlplane/global_behavior_profile.py`
- `lintgate/pattern_bank.py`
- `lintgate/controlplane/model_profiles.py`
- `tests/test_global_behavior_profile.py`
- `tests/test_signal_decomposition.py` (new)

## Implementation contract
1. Add `SignalSourceDecomposition` schema:
   - `model_baseline`, `codebase_baseline`, `session_deviation`, `significance`, `source_attribution`
2. Extend pattern-bank persistence to track behavior signal recurrence baselines (additive format only).
3. Implement deterministic attribution logic:
   - `model_tendency`
   - `codebase_tendency`
   - `session_anomaly`
   - `genuine_signal`
4. Expose decomposition helper API usable by behavior channel.

## Adversarial requirements
- Sparse/no-history inputs must fall back to conservative `session_anomaly` classification.
- Attribution must be deterministic with explicit tie-break ordering.

## Acceptance commands
```bash
pytest tests/test_global_behavior_profile.py tests/test_signal_decomposition.py -q
python - <<'PY'
from lintgate.controlplane.global_behavior_profile import GlobalBehaviorProfile
print(hasattr(GlobalBehaviorProfile, 'to_dict'))
PY
```
Expected:
- Tests pass.
- Behavior baselines persist without breaking existing pattern bank loads.

## Dependency metadata
- `depends_on`: none
- `parallel_safe_with`: #157, #160, #162, #168
- `conflicts_with`: concurrent edits to `lintgate/controlplane/global_behavior_profile.py`

## Out of scope
- Confidence modulation in behavior findings.

=== ISSUE 167 ===
title:	P2: Apply source attribution to behavior finding confidence and diagnostics
state:	OPEN
author:	rohanvinaik
labels:	area:quality, controlplane, metrics, P2, type:enhancement
comments:	0
assignees:	
projects:	
milestone:	
number:	167
--
## Parent
Part of #156 (Phase 2: signal significance discrimination).

## Category
A (execution-ready) — behavior-channel integration.

## One Deliverable
Integrate source attribution into behavior finding confidence and emitted diagnostics.

## Scope (files likely touched)
- `lintgate/channels/behavior_channel.py`
- `tests/test_behavior_channel.py`
- `tests/test_signal_decomposition.py`

## Implementation contract
1. Apply decomposition output to confidence modulation:
   - `model_tendency`: -0.20
   - `codebase_tendency`: -0.15
   - `session_anomaly`: low-confidence marker
   - `genuine_signal`: unchanged or boosted
2. Add `signal_decomposition` metrics block per behavior run.
3. Preserve existing detection rule firing semantics; change only confidence/metadata.
4. Ensure deterministic modulation for identical input state.

## Adversarial requirements
- Confidence adjustments must be clamped to [0.0, 1.0].
- Missing decomposition data must preserve previous confidence behavior.

## Acceptance commands
```bash
pytest tests/test_behavior_channel.py tests/test_signal_decomposition.py -q
python - <<'PY'
from lintgate.channels.behavior_channel import BehaviorChannel
print(callable(BehaviorChannel().execute))
PY
```
Expected:
- Tests pass.
- Behavior metrics include `signal_decomposition` output.

## Dependency metadata
- `depends_on`: #166
- `parallel_safe_with`: #168
- `conflicts_with`: concurrent edits to `lintgate/channels/behavior_channel.py`

## Out of scope
- Authority escalation changes.

=== ISSUE 168 ===
title:	P2: Define bounded session transfer packet generation and persistence
state:	OPEN
author:	rohanvinaik
labels:	area:quality, controlplane, metrics, P2, type:enhancement
comments:	0
assignees:	
projects:	
milestone:	
number:	168
--
## Parent
Part of #156 (Phase 6: cross-session pattern transfer protocol).

## Category
A (execution-ready) — transfer packet schema + persistence.

## One Deliverable
Define and persist bounded `SessionTransferPacket` on session expiry and explicit save checkpoints.

## Scope (files likely touched)
- `lintgate/orchestration/session_transfer.py` (new)
- `lintgate/controlplane/session_memory.py`
- `tests/test_session_transfer.py` (new)
- `tests/test_session_memory.py`

## Implementation contract
1. Add `SessionTransferPacket` schema with bounded fields for:
   - top resolutions
   - stuck patterns
   - authority history
   - compliance rates
   - signal decompositions
   - context summary
2. Generate packet on session expiry path and explicit save path.
3. Enforce serialized packet size bound (<2KB) via deterministic trimming order.
4. Store per-project transfer packet history with bounded retention (LRU).

## Adversarial requirements
- Corrupt prior transfer packets must fail-open.
- Packet generation must tolerate missing optional sections (e.g., no resolutions).

## Acceptance commands
```bash
pytest tests/test_session_memory.py tests/test_session_transfer.py -q
python - <<'PY'
from lintgate.orchestration.session_transfer import SessionTransferPacket
print(hasattr(SessionTransferPacket, '__dataclass_fields__'))
PY
```
Expected:
- Tests pass.
- Session expiry writes bounded transfer packet artifacts.

## Dependency metadata
- `depends_on`: none
- `parallel_safe_with`: #157, #160, #162, #166
- `conflicts_with`: concurrent edits to `lintgate/controlplane/session_memory.py`

## Out of scope
- Session-start preload into behavior/authority pipelines.

=== ISSUE 169 ===
title:	P2: Preload transfer packets on session start to warm repertoire/authority/compliance
state:	OPEN
author:	rohanvinaik
labels:	area:agent-integration, controlplane, metrics, P2, type:enhancement
comments:	0
assignees:	
projects:	
milestone:	
number:	169
--
## Parent
Part of #156 (Phase 6: cross-session pattern transfer protocol).

## Category
A (execution-ready) — runtime preload integration.

## One Deliverable
Load transfer packets at session start and prewarm repertoire/authority/compliance state.

## Scope (files likely touched)
- `lintgate/channels/behavior_channel.py`
- `lintgate/orchestration/authority.py`
- `lintgate/orchestration/resolution_repertoire.py`
- `lintgate/orchestration/session_transfer.py`
- `tests/test_behavior_channel.py`
- `tests/test_authority_escalation.py`
- `tests/test_session_transfer.py`

## Implementation contract
1. On new session startup, load most recent packet for project (optionally model-specific preference if available).
2. Prewarm:
   - repertoire query cache
   - initial authority floor per signal
   - compliance rate priors
3. Add deterministic fallback if no packet exists.
4. Expose preload result in behavior metrics metadata (`transfer_loaded`, `packet_age_hours`).

## Adversarial requirements
- Stale packets beyond TTL must be ignored.
- Model-key mismatch must not contaminate model-specific priors.

## Acceptance commands
```bash
pytest tests/test_behavior_channel.py tests/test_authority_escalation.py tests/test_session_transfer.py -q
python - <<'PY'
print(True)
PY
```
Expected:
- Tests pass.
- New sessions reuse prior successful patterns and escalation baselines when valid packet exists.

## Dependency metadata
- `depends_on`: #168, #165, #163
- `parallel_safe_with`: #170
- `conflicts_with`: concurrent edits to `lintgate/channels/behavior_channel.py`

## Out of scope
- MCP/CLI status exposure of transfer packets.

=== ISSUE 170 ===
title:	P2: Expose transfer and delivery health status through MCP observability surfaces
state:	OPEN
author:	rohanvinaik
labels:	area:agent-integration, controlplane, developer-experience, P2, type:enhancement
comments:	0
assignees:	
projects:	
milestone:	
number:	170
--
## Parent
Part of #156 (Phase 6: cross-session pattern transfer protocol).

## Category
A (execution-ready) — observability and operator visibility.

## One Deliverable
Expose transfer-packet and behavioral-delivery status through MCP observability tools.

## Scope (files likely touched)
- `mcp_tools/behavior_tools.py`
- `mcp_tools/controlplane_tools.py`
- `tests/test_mcp_behavior_tools.py`
- `tests/test_controlplane_tools.py`
- `tests/test_session_transfer.py`

## Implementation contract
1. Extend `global_memory_status` (and/or controlplane status output) with transfer telemetry:
   - `latest_transfer_packet`
   - `packet_age_hours`
   - `resolutions_available`
   - `suppressed_nudges`
2. Add optional detail section for delivery channel health summary:
   - channels used
   - fallback counts
   - reliability tier distribution
3. Keep output additive and compact-safe.
4. Avoid exposing raw sensitive command/output content.

## Adversarial requirements
- Missing transfer artifacts must return explicit `status: unavailable`, not exceptions.
- Large status payloads must remain bounded in compact mode.

## Acceptance commands
```bash
pytest tests/test_mcp_behavior_tools.py tests/test_controlplane_tools.py tests/test_session_transfer.py -q
python - <<'PY'
print(True)
PY
```
Expected:
- Tests pass.
- Operators can inspect transfer and delivery health without opening raw state files.

## Dependency metadata
- `depends_on`: #168
- `parallel_safe_with`: #169
- `conflicts_with`: concurrent edits to `mcp_tools/behavior_tools.py`

## Out of scope
- Training data extraction changes in NSIL modules.

