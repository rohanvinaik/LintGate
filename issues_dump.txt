================ ISSUE 78 ================
title:	Add explicit silent-verification detector and policy gate to test-effectiveness
state:	OPEN
author:	rohanvinaik
labels:	
comments:	1
assignees:	
projects:	
milestone:	
number:	78
--
## Summary
The system can report functions with multiple tests but zero semantic assertions, yet this often remains advisory and easy to ignore. Add an explicit detector/gate path for "silent verification" patterns.

## Strategic Context
This closes the loop between diagnosis and enforcement: if tests execute but do not verify value/state, quality signals should escalate predictably.

## Proposed Implementation Plan
1. Extend test-effectiveness output with silent-verification metrics:
- count of functions where `test_count > 0` and `assertion_count == 0`
- prioritized list of critical-path offenders
2. Add configurable policy threshold:
- warning/blocking mode for critical modules (hooks/controlplane/runtime)
- advisory mode for the rest during rollout
3. Emit actionable findings:
- include concrete assertion hardening suggestions tied to function behavior category
4. Add tests for policy evaluation and output schema.

## Acceptance Criteria
- Silent-verification cases are machine-detectable and surfaced consistently.
- Policy can be configured by scope/severity.
- Critical-path functions can fail gates when verification is effectively absent.


================ ISSUE 77 ================
title:	Add property-based verification gates for pure-core analyzers (assertion_classifier/source_mapper)
state:	OPEN
author:	rohanvinaik
labels:	
comments:	1
assignees:	
projects:	
milestone:	
number:	77
--
## Summary
The codebase has a large pure-function surface, but pure-core analyzers still rely mostly on hand-authored examples. Property-based verification is underused where it provides maximum leverage.

## Strategic Context
This is the "untapped purity" gap: pure logic engines (`assertion_classifier`, `source_mapper`) can be stress-tested with generated inputs to catch edge cases missed by example-based tests.

## Proposed Implementation Plan
1. Add Hypothesis-based test suites for:
- `lintgate/linters/test_effectiveness/assertion_classifier.py`
- `lintgate/linters/test_effectiveness/source_mapper.py`
2. Define invariants and metamorphic properties:
- Classifier: deterministic output for same AST, bounded strength values, stable taxonomy invariants.
- Mapper: deterministic mappings for same inputs, diagnostics consistency invariants (counts/rates bounds), no duplicate symbol corruption.
3. Start with high-complexity pure functions first (top 10 by complexity/purity metrics).
4. Add gating profile:
- Run fast property pack in normal CI.
- Keep deeper examples under optional extended profile if needed.

## Acceptance Criteria
- Property tests cover both classifier and mapper core paths.
- Edge-case regressions are reproducible with shrunk counterexamples.
- TEFF semantic-correctness confidence improves with invariant coverage evidence.


================ ISSUE 76 ================
title:	Replace dynamic mcp_server tool exports with explicit typed ToolRegistry contract
state:	OPEN
author:	rohanvinaik
labels:	
comments:	1
assignees:	
projects:	
milestone:	
number:	76
--
## Summary
`mcp_server.py` still uses dynamic export mutation (`globals().update(_tool_funcs)`), which creates type/import fragmentation across consumers and static analyzers.

## Strategic Context
This is the structural root of the "hollow cognition" pattern: advanced runtime behavior with weak static contracts. It contributes to unresolved-import / attr-defined churn and makes tool surfaces hard to reason about statically.

## Evidence
- `mcp_server.py` uses:
  - `_tool_funcs = register_all(mcp, _helpers)`
  - `globals().update(_tool_funcs)`
- Consumer surfaces (notably cold-start tests and static checks) depend on discoverable top-level symbols.

## Proposed Implementation Plan
1. Introduce an explicit typed tool contract:
- Define a `ToolRegistry` protocol/dataclass in a stable module (for example `mcp_tools/registry_types.py`).
- Enumerate required callable signatures for exported tools.
2. Replace dynamic global mutation with deterministic exports:
- Keep runtime registration for MCP, but expose explicit wrapper functions or a static export map in `mcp_server.py`.
- Populate `__all__` with supported public tool symbols.
3. Add compatibility layer:
- If transitional dynamic lookup is needed, gate it behind deprecation path with warning.
4. Add static and runtime verification:
- Tests asserting exported symbol set stability.
- Type-check/lint contract tests for tool importability from `mcp_server`.

## Acceptance Criteria
- `globals().update(...)` removed from production path.
- Tool exports are statically discoverable and typed.
- Cold-start/import surfaces no longer depend on dynamic mutation semantics.


================ ISSUE 75 ================
title:	Re-ground agent context docs: resolve contradictions and dead-path references
state:	OPEN
author:	rohanvinaik
labels:	
comments:	0
assignees:	
projects:	
milestone:	
number:	75
--
## Summary
Agent-facing context docs need re-grounding: contradictory project-value statements and dead-path references (for example missing docs like `CONVENTIONS.md`) degrade guidance fidelity.

## Strategic Context
When context is stale or contradictory, agents optimize against a false workspace map. This creates systematic reasoning drift even when code tooling is strong.

## Proposed Implementation Plan
1. Audit and reconcile core context docs:
- `CLAUDE.md`
- related onboarding/context bootstrap docs
2. Remove or repair dead references:
- replace missing-path links with valid paths
- delete obsolete references where no replacement exists
3. Add machine-checkable context health validation:
- test/script that verifies referenced local paths exist
- optional contradiction lint for duplicated/conflicting policy statements
4. Integrate into quality workflow:
- run context health check in CI/preflight as non-blocking initially, then escalate.

## Acceptance Criteria
- No dead local-path references in primary context docs.
- Contradictory policy blocks are resolved to a single canonical statement.
- Context-health check produces deterministic pass/fail output.


================ ISSUE 74 ================
title:	Semantic assertion hardening for hook/control-plane persistence paths
state:	OPEN
author:	rohanvinaik
labels:	
comments:	1
assignees:	
projects:	
milestone:	
number:	74
--
## Summary
Core hook-path tests still over-index on non-crash behavior and weak assertions. High-vulnerability functions have multiple tests but effectively zero semantic verification.

## Strategic Context
This is the "silent verification" failure mode: execution is tested, correctness is not. It materially depresses test-effectiveness and allows value-level regressions to survive.

## Priority Targets
Start with top vulnerable hook/control-plane functions (for example `save_run_details`, `refresh_runtime_after_run`, related habit/runtime persistence paths).

## Proposed Implementation Plan
1. Build a focused hardening pack for hook-path tests:
- `tests/test_hook_controlplane.py`
- `tests/test_hook_habit_cov.py` (or equivalent hook-habit suite)
2. Replace weak checks with semantic/state assertions:
- Assert persisted JSON payload schema and key value invariants.
- Assert state-transition outcomes, not just call counts/no exceptions.
3. Add regression-style invariants:
- For persistence routines, assert deterministic key presence and content constraints.
- Ensure failure-path behavior is explicitly asserted (not silently ignored).
4. Measure impact:
- Re-run `analyze_test_strength` and capture vulnerability deltas for targeted functions.

## Acceptance Criteria
- Targeted hook-path functions no longer show test_count>0 with assertion_count=0.
- Mutation vulnerability for priority functions decreases materially.
- Tests fail on value-level regressions, not only crashes.


================ ISSUE 73 ================
title:	structure_channel split regressed helper symbol import compatibility
state:	OPEN
author:	rohanvinaik
labels:	
comments:	1
assignees:	
projects:	
milestone:	
number:	73
--
## Summary
The `structure_channel` modularization into `structure_logic.py` removed helper symbol availability from `lintgate.channels.structure_channel`, breaking existing imports used by test and tooling surfaces.

## Reproduction
Run:
- `pytest -q tests/test_structure_channel.py`

Observed:
- `ImportError: cannot import name '_build_reexport_map' from 'lintgate.channels.structure_channel'`

## Evidence in Code
- `lintgate/channels/structure_channel.py` now imports only a subset from `structure_logic` and no longer exposes helper symbols previously importable from this module.
- `lintgate/channels/structure_logic.py` contains `_build_reexport_map`, `_detect_reexports`, `_is_orphan_excluded`, etc.

## Impact
- Existing white-box test and integration imports fail at module import time.
- Increases migration risk for downstream tooling that imported these helpers from the prior path.

## Proposed Implementation Plan
1. Add explicit compatibility re-exports in `structure_channel.py` for helper symbols historically imported from that module.
2. Alternatively, if contract is intentionally changed, provide deprecation shim plus one release of backward compatibility.
3. Update tests/documentation to target canonical module after compatibility window.

## Acceptance Criteria
- `tests/test_structure_channel.py` imports and runs successfully.
- No functional regression in structure channel runtime behavior.
- Helper import path migration is explicit and non-breaking.


================ ISSUE 72 ================
title:	setup_github_quality extraction introduced response-schema and status-semantics regressions
state:	OPEN
author:	rohanvinaik
labels:	
comments:	1
assignees:	
projects:	
milestone:	
number:	72
--
## Summary
The extracted `mcp_tools/setup_github_quality.py` changed response contract keys and some status semantics, causing broad regression in existing setup/onboarding expectations.

## Reproduction
Run:
- `pytest -q tests/test_setup_github_quality.py`

Current failures (sample):
- missing top-level `status`
- missing expected keys like `codeclimate` (now `codeclimate_yml`)
- `badges.status` mismatch when no GitHub remote
- `scanner` object/status mismatch (`None` or `error` instead of expected preview/scanner_not_found semantics)
- `qlty` payload missing expected fields like `local_only`

## Evidence in Code
- `mcp_tools/setup_github_quality.py` currently emits loop-generated keys (`codeclimate_yml`, `sonar_properties`, etc.) and omits legacy envelope/status fields.
- Sonar scanner branch sets `{"status": "error"}` for not-found scanner, differing from expected explicit `scanner_not_found` semantics.

## Impact
- MCP contract drift for consumers that parse setup results.
- Breakage across onboarding and quality setup verification flows.

## Proposed Implementation Plan
1. Reintroduce stable response envelope:
- top-level `status` (`preview`/`written`)
- legacy artifact aliases (`codeclimate`, `sonar`, etc.) mapped additively from new internal keys.
2. Normalize status semantics to prior contract where applicable:
- no-GitHub remote badge branch
- scanner preview/write states (`preview`, `scanner_not_found`, etc.)
3. Preserve loop-driven modular internals while adapting final response translation layer.
4. Add compatibility tests (existing `tests/test_setup_github_quality.py`) plus schema-lock test for response keys.

## Acceptance Criteria
- `tests/test_setup_github_quality.py` passes.
- Existing consumers can continue reading prior response keys/statuses.
- New internal modularization remains intact behind compatibility layer.


================ ISSUE 71 ================
title:	getting_started refactor regressed startup_setup contract and helper fallback behavior
state:	OPEN
author:	rohanvinaik
labels:	
comments:	1
assignees:	
projects:	
milestone:	
number:	71
--
## Summary
Recent onboarding refactor changed the `getting_started` response shape and helper assumptions in ways that break existing callers and tests.

## Reproduction
Run:
- `pytest -q tests/test_cold_start.py tests/test_onboarding_helpers.py`

Observed failures include:
- `startup_setup.missing_tools_before` missing
- `startup_setup.missing_tools_after` missing
- `startup_setup.github_quality` missing
- `KeyError: '_json_dumps'` when `register(...)` is called with minimal helper dicts

## Evidence in Code
- `mcp_tools/onboarding_tools.py`
  - `getting_started` builds `startup_setup` without `missing_tools_before/after` and without persisting `_handle_quality_bootstrap(...)` result.
  - return path unconditionally calls `helpers["_json_dumps"](...)`.

## Impact
- Cold-start UX/data contract drift for MCP consumers.
- Test harnesses and thin integrations using minimal helper shims fail.

## Proposed Implementation Plan
1. Restore additive compatibility fields in `startup_setup`:
- `missing_tools_before`
- `missing_tools_after`
- `github_quality`
2. Persist `_handle_quality_bootstrap(...)` return into output payload.
3. Make serialization resilient:
- if `_json_dumps` helper missing, fall back to `json.dumps(..., indent=2)`.
4. Keep existing refactor structure; only restore output contract and fallback behavior.
5. Add regression tests for payload keys and helper fallback.

## Acceptance Criteria
- `tests/test_cold_start.py` and `tests/test_onboarding_helpers.py` pass for getting_started payload paths.
- Existing fields remain; restored fields are additive (no consumer break).


================ ISSUE 70 ================
title:	Bound test_effectiveness runtime with deterministic partial-analysis fallback
state:	OPEN
author:	rohanvinaik
labels:	
comments:	1
assignees:	
projects:	
milestone:	
number:	70
--
## Summary
`test_effectiveness` can push ControlPlane into degraded mode on very large trees. Current analysis is holistic and can exceed practical runtime budgets in big/clone-heavy repositories.

## Evidence
Retrospective runs reported degraded coherence when test_effectiveness timed out on large scan scopes.

## Proposed Implementation Plan
1. Add explicit runtime budget controls to TEFF analysis:
- max analysis wall-time
- optional max source/test file budgets
- deterministic prioritization order when budget is hit
2. Return partial-result metadata instead of hard failure:
- `analysis_truncated: true`
- scanned vs total counts
- confidence downgrade in channel summary
3. Surface budget settings through channel/tool config with sane defaults.
4. Add tests for budget-hit behavior to ensure deterministic, non-crashing degraded output.

## Acceptance Criteria
- Large repositories no longer hard-error TEFF channel under budget pressure.
- ControlPlane receives explicit partial-analysis metadata and coherent severity/confidence.
- Runtime is bounded and reproducible under fixed budgets.


================ ISSUE 69 ================
title:	Generalize discovery excludes for nested subprojects/clones (beyond named mutation dirs)
state:	OPEN
author:	rohanvinaik
labels:	
comments:	1
assignees:	
projects:	
milestone:	
number:	69
--
## Summary
Discovery/orphan analysis still relies mostly on static directory-name excludes. This misses generic "project-inside-project" clones (nested repos/subprojects), which can inflate scan scope and create structural noise.

## Evidence
The retrospective notes that artifact/clone trees can dominate structural findings and degrade channel coherence on large trees. Current discovery paths exclude known names (`mutants`, `.mutmut`, etc.) but do not automatically exclude arbitrary nested repo roots.

## Proposed Implementation Plan
1. Add nested-subproject detection heuristics during discovery:
- detect directories containing markers like `.git`, `pyproject.toml`, or `package.json` that are not the top project root
- optionally gate with size threshold / import-isolation heuristics to avoid false exclusions
2. Thread this exclusion set through:
- structure discovery/orphan analysis
- test_effectiveness source/test discovery
3. Expose diagnostics in channel metrics:
- `auto_excluded_subprojects` list
- count of files skipped by heuristic
4. Add config overrides:
- allowlist to include a nested subproject
- blocklist to force-exclude known paths
5. Add tests with synthetic nested repo fixtures.

## Acceptance Criteria
- Nested clone/subproject trees are excluded by default without hardcoding specific names.
- Structural noise and scan time are reduced on monorepo-like trees.
- Users can override detection when needed.


================ ISSUE 68 ================
title:	Add explicit TEFF cache schema versioning and fail-open invalidation
state:	OPEN
author:	rohanvinaik
labels:	
comments:	1
assignees:	
projects:	
milestone:	
number:	68
--
## Summary
The TEFF manifest cache has no explicit schema/version contract, so structural refactors can silently invalidate cache compatibility and surface as runtime failures instead of safe cache invalidation.

## Why This Matters
Recent diagnostics refactors changed data model shape. Without schema negotiation, deserialization relies on implicit assumptions and can break end-to-end tooling.

## Proposed Implementation Plan
1. Introduce explicit cache envelope metadata in `lintgate/linters/test_effectiveness/manifest.py`:
- `schema_version`
- optional `generator_version` and `written_at`
2. On load, if schema is missing or incompatible:
- treat cache as stale
- return empty manifest metadata and force rebuild (fail-open)
3. Keep backward compatibility path:
- accept legacy cache format during migration window
- rewrite in new envelope on next successful save
4. Add tests for:
- matching schema load
- schema mismatch invalidation/rebuild
- legacy schema read then rewrite

## Acceptance Criteria
- Incompatible cache payloads do not error tool/channel execution.
- Cache compatibility behavior is explicit and deterministic.
- Schema transitions are covered by tests.


================ ISSUE 66 ================
title:	Normalize mapping diagnostics to improve hint fidelity (raw counters are currently over-amplified)
state:	OPEN
author:	rohanvinaik
labels:	
comments:	5
assignees:	
projects:	
milestone:	
number:	66
--
## Scope
Phase 1 foundational work for normalized mapper diagnostics.

## Dependency placement
- Phase: **1**
- Parallel with: #63
- Supports: #56
- Depends on: none (can build on #63 data model if landed first)

## Goal
Add **normalized and deduplicated diagnostics** so hinting logic is based on meaningful rates, not only raw attempt counts.

## Implementation guidance
1. Extend `MappingDiagnostics` in `lintgate/linters/test_effectiveness/types.py` with additive fields:
- `unique_symbols_attempted` (derived from internal set length)
- `unique_symbols_mapped` (derived from internal set length)
- `test_functions_examined`
- `top_drop_examples` (top-N entries `{symbol, reason, strategy}`)
- `dominant_drop_reason`
- `dominant_drop_pct`

2. Track non-serialized internal state safely.
- Use internal sets/lists with `field(default_factory=...)` for unique tracking.
- Do not serialize raw set objects; only expose derived scalar/list outputs.

3. Compute normalized diagnostics in mapper flow.
- In `map_tests_to_source`, update test-function examined count.
- Track unique attempted/mapped symbols.
- Record drop examples and truncate to top 5.
- Compute dominant drop reason and pct over total drops.

4. Document semantics.
- Clarify in code/docs that strategy attempt sums may exceed aggregate test-function attempts due multi-strategy evaluation.

## Test guidance
Add/update tests in `tests/test_source_mapper.py`:
- `test_diagnostics_unique_symbol_counts`
- `test_diagnostics_top_drop_examples`
- `test_diagnostics_dominant_drop_reason`
- `test_diagnostics_normalized_vs_raw` (`unique_symbols_attempted <= attempted` invariant or documented alternative metric relation)

## Acceptance criteria
- Diagnostics include both raw and normalized fields.
- Dominant reason and pct are present when drops exist.
- Serialization remains backward compatible.


================ ISSUE 63 ================
title:	Refactor source mapping into staged, diagnosable resolution to harden semantic correctness
state:	OPEN
author:	rohanvinaik
labels:	
comments:	5
assignees:	
projects:	
milestone:	
number:	63
--
## Scope
Phase 1 foundational work for mapper explainability.

## Dependency placement
- Phase: **1**
- Parallel with: #66
- Blocks: #56
- Depends on: none

## Goal
Introduce **strategy-scoped mapping diagnostics plumbing** so mapping outcomes are attributable to specific resolution paths.

## Implementation guidance
1. Add strategy diagnostics primitives in `lintgate/linters/test_effectiveness/types.py`.
- Add `StrategyDiagnostics` dataclass with at least:
  - `strategy`
  - `attempted`, `matched`
  - `dropped_ambiguous`, `dropped_no_candidate`, `dropped_shadowed`
- Extend `MappingDiagnostics` with additive field:
  - `strategy_breakdown: dict[str, StrategyDiagnostics]`
- Keep existing fields (`attempted`, `mapped`, `dropped_*`) unchanged for backward compatibility.

2. Thread strategy labels through mapper resolution in `lintgate/linters/test_effectiveness/source_mapper.py`.
- Extend `_try_add_match(...)` to accept `strategy: str`.
- Pass explicit strategy labels from callers:
  - call graph path: `"call_graph"`
  - naming path: `"naming"`
  - alias/import symbol path: `"alias_import"` (or documented equivalent)
- Update diagnostics so each attempt/match/drop updates both:
  - aggregate counters (existing behavior)
  - strategy-scoped counters (new behavior)

3. Keep serialization additive.
- `MappingDiagnostics.to_dict()` must include new keys additively.
- Existing consumers reading old keys should continue to work unchanged.

## Test guidance
Add/update tests in `tests/test_source_mapper.py`:
- `test_diagnostics_strategy_breakdown_call_graph`
- `test_diagnostics_strategy_breakdown_naming`
- backward-compat serialization assertion: old keys remain present/correct.

## Acceptance criteria
- Strategy-level counters are emitted and non-empty on mixed mapping fixtures.
- Aggregate counters still match prior behavior.
- No breaking changes to existing serialized keys.


================ ISSUE 56 ================
title:	analyze_test_strength still returns generic zero-mapping guidance for narrow/ambiguous scopes
state:	OPEN
author:	rohanvinaik
labels:	
comments:	8
assignees:	
projects:	
milestone:	
number:	56
--
## Scope
Phase 3 messaging/state consistency.

## Dependency placement
- Phase: **3**
- Depends on: #63, #66

## Goal
Replace contradictory zero-function messaging with explicit analysis states driven by enriched diagnostics.

## Implementation guidance
1. Introduce explicit analysis-state model.
- Add `AnalysisState` enum in `lintgate/linters/test_effectiveness/types.py`:
  - `success`
  - `no_source_symbols`
  - `mappings_found_but_no_analyzable_public_functions`
  - `no_mappings`
  - `no_files`

2. Update `analyze_test_strength` outcome branching in `mcp_tools/test_effectiveness_tools.py`.
- Replace generic `not manifest.functions` fallback with state-specific conditions.
- Ensure **consistency rule**: if diagnostics report `mapped > 0`, do not emit "No mapped functions analyzed".

3. Use dominant-drop diagnostics for hints.
- For `no_mappings`, select hint based on normalized dominant cause from phase-1 diagnostics (not raw if/elif only).

4. Emit machine-readable state always.
- Success path: `analysis_state: "success"`.
- Non-success paths: state + state-specific note + diagnostics.

## Test guidance
Update/add in `tests/test_test_effectiveness_tools.py`:
- `test_analysis_state_success`
- `test_analysis_state_no_source_symbols`
- `test_analysis_state_no_mappings`
- `test_analysis_state_mappings_no_analyzable`
- `test_no_contradictory_state`
- Update legacy `test_zero_mapping_hints` to assert state-driven behavior (not fixed string only).

## Acceptance criteria
- State is explicit and non-contradictory in all paths.
- Hints are cause-specific and stable.
- Existing API shape remains backward compatible with additive fields.


