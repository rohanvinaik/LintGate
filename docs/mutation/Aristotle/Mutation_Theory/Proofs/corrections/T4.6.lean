/-
This file was generated by Aristotle (https://aristotle.harmonic.fun).

Lean version: leanprover/lean4:v4.24.0
Mathlib version: f897ebcf72cd16f89ab4577d0c826cd14afaafc7
This project request had uuid: 3a6a020a-d426-4da1-b198-59728d9275a0

To cite Aristotle, tag @Aristotle-Harmonic on GitHub PRs/issues, and add as co-author to commits:
Co-authored-by: Aristotle (Harmonic) <aristotle-harmonic@harmonic.fun>
-/

/-
This module proves Pinsker's inequality lemma C, specifically the Cauchy-Schwarz step.
It defines `MutDistribution` and `is_distribution` and proves `T4_6_lemmaC_pinsker_cauchy_schwarz`
using `Finset.sum_mul_sq_le_sq_mul_sq`.
-/

import Mathlib

set_option linter.mathlibStandardSet false

open scoped BigOperators
open scoped Real
open scoped Nat
open scoped Classical
open scoped Pointwise

set_option maxHeartbeats 0
set_option maxRecDepth 4000
set_option synthInstance.maxHeartbeats 20000
set_option synthInstance.maxSize 128

set_option relaxedAutoImplicit false
set_option autoImplicit false

noncomputable section

/-
Definitions of MutDistribution and is_distribution.
-/
def MutDistribution (α : Type*) := α → ℝ

def is_distribution {α : Type*} [Fintype α] (P : MutDistribution α) : Prop :=
  (∀ x, 0 ≤ P x) ∧ (∑ x, P x = 1)

/-
Pinsker's inequality lemma C: Cauchy-Schwarz step.
-/
lemma T4_6_lemmaC_pinsker_cauchy_schwarz {α : Type} [Fintype α] (P Q : MutDistribution α) (hP : is_distribution P) (hQ : is_distribution Q) (hQ_pos : ∀ x, 0 < Q x) :
  (∑ x, |P x - Q x|)^2 ≤ 3 * ∑ x, (P x - Q x)^2 / (P x + 2 * Q x) := by
    -- Applying the Cauchy-Schwarz inequality in its generalized form for sequences in Euclidean space.
    have h_cauchy_schwarz : ∀ (u v : α → ℝ), (∑ x, u x * v x)^2 ≤ (∑ x, u x^2) * (∑ x, v x^2) := by
      exact?;
    convert h_cauchy_schwarz ( fun x => |P x - Q x| / Real.sqrt ( P x + 2 * Q x ) ) ( fun x => Real.sqrt ( P x + 2 * Q x ) ) using 1 <;> norm_num [ Real.sq_sqrt ( add_nonneg ( hP.1 _ ) ( mul_nonneg zero_le_two ( le_of_lt ( hQ_pos _ ) ) ) ) ];
    · exact congr_arg ( · ^ 2 ) ( Finset.sum_congr rfl fun x _ => by rw [ div_mul_cancel₀ _ ( ne_of_gt ( Real.sqrt_pos.mpr ( add_pos_of_nonneg_of_pos ( hP.1 x ) ( mul_pos zero_lt_two ( hQ_pos x ) ) ) ) ) ] );
    · norm_num [ div_pow, Real.sq_sqrt ( add_nonneg ( hP.1 _ ) ( mul_nonneg zero_le_two ( le_of_lt ( hQ_pos _ ) ) ) ) ];
      simp_all +decide [ Finset.sum_add_distrib, two_mul, add_assoc, is_distribution ] ; ring